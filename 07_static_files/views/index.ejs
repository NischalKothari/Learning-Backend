<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Random Web Template</title>
  <style>
    :root {
      --bg: #0f1220;
      --panel: #161a2e;
      --text: #e8eaf6;
      --muted: #b5bad6;
      --brand: #7c5cff;
      --brand-2: #30e0a1;
      --ring: rgba(124,92,255,.35);
      --shadow: 0 10px 25px rgba(0,0,0,.35);
    }
    :root.light {
      --bg: #f7f8fd;
      --panel: #ffffff;
      --text: #1a1c2b;
      --muted: #555a7a;
      --brand: #6348ff;
      --brand-2: #16b87f;
      --ring: rgba(99,72,255,.25);
      --shadow: 0 8px 18px rgba(13, 18, 33, .08);
    }

    *, *::before, *::after { box-sizing: border-box; }
    html, body { height: 100%; }
    body {
      margin: 0; font-family: system-ui, -apple-system, Segoe UI, Roboto, Ubuntu, Cantarell, "Helvetica Neue", Arial, "Noto Sans", "Apple Color Emoji", "Segoe UI Emoji";
      background: radial-gradient(1200px 600px at 10% -10%, rgba(124,92,255,.15), transparent 50%),
                  radial-gradient(1200px 600px at 110% 10%, rgba(48,224,161,.12), transparent 50%),
                  var(--bg);
      color: var(--text);
      line-height: 1.5;
    }

    a { color: inherit; text-decoration: none; }
    img { max-width: 100%; display: block; }

    .container { width: min(1100px, 92vw); margin: 0 auto; }

    /* Header */
    header {
      position: sticky; top: 0; z-index: 50;
      backdrop-filter: saturate(120%) blur(8px);
      background: color-mix(in srgb, var(--bg) 65%, transparent);
      border-bottom: 1px solid color-mix(in srgb, var(--panel) 60%, var(--text) 5%);
    }
    .nav {
      display: flex; align-items: center; justify-content: space-between;
      padding: .9rem 0;
    }
    .brand { display:flex; align-items:center; gap:.6rem; font-weight: 700; letter-spacing:.3px; }
    .brand .dot { width: 10px; height: 10px; border-radius: 50%; background: linear-gradient(45deg, var(--brand), var(--brand-2)); box-shadow: 0 0 0 6px var(--ring); }
    .nav a { opacity:.9 }
    .nav a:hover { opacity: 1; }
    .nav .links { display:flex; gap: 1rem; align-items: center; }
    .nav .btn { padding:.55rem .9rem; border-radius: 12px; background: linear-gradient(135deg, var(--brand), var(--brand-2)); color: white; font-weight: 600; box-shadow: var(--shadow); border: none; cursor:pointer; }
    .nav .ghost { background: transparent; border:1px solid color-mix(in srgb, var(--brand) 55%, var(--text) 15%); color: var(--text); }

    .menu-toggle { display:none; border:0; background:transparent; color:var(--text); font-size:1.3rem; }
    @media (max-width: 720px) {
      .nav .links { display:none; }
      .menu-toggle { display:block; }
      .mobile-menu { display:none; }
      .mobile-menu.open { display:flex; flex-direction:column; gap:.8rem; padding: .8rem 0 1rem; }
      .mobile-menu a, .mobile-menu button { width: 100%; text-align:left; }
    }

    /* Hero */
    .hero { padding: 5rem 0 3rem; }
    .hero .wrap { display:grid; grid-template-columns: 1.2fr 1fr; gap: 2rem; align-items: center; }
    .title { font-size: clamp(2rem, 4vw, 3.1rem); line-height: 1.1; margin: 0 0 .6rem; }
    .subtitle { color: var(--muted); margin: 0 0 1.2rem; }
    .cta { display:flex; gap: .75rem; flex-wrap: wrap; }
    .btn {
      padding:.9rem 1.1rem; border-radius: 14px; background: linear-gradient(135deg, var(--brand), var(--brand-2)); color: white; font-weight: 700; border: none; cursor: pointer; box-shadow: var(--shadow);
    }
    .btn.ghost { background:transparent; color: var(--text); border:1px solid color-mix(in srgb, var(--brand) 55%, var(--text) 15%); }

    .hero-card { background: var(--panel); border:1px solid color-mix(in srgb, var(--panel) 70%, var(--text) 6%); padding: 1rem; border-radius: 18px; box-shadow: var(--shadow); }
    .stat { display:grid; grid-template-columns: auto 1fr; gap:.5rem 1rem; align-items:center; padding:.65rem .75rem; border-radius: 12px; background: color-mix(in srgb, var(--panel) 85%, var(--text) 4%); }
    .stat .ico { width: 38px; height: 38px; border-radius: 10px; display:grid; place-items:center; background: linear-gradient(135deg, var(--brand), var(--brand-2)); font-weight:900; }
    .stat strong { font-size: 1.05rem; }
    .stat small { color: var(--muted); }

    @media (max-width: 900px){ .hero .wrap { grid-template-columns: 1fr; } }

    /* Features */
    section { padding: 3.5rem 0; }
    .section-title { font-size: clamp(1.5rem, 3.2vw, 2.2rem); margin: 0 0 .6rem; }
    .section-sub { color: var(--muted); margin: 0 0 2rem; }

    .grid { display:grid; gap: 1rem; grid-template-columns: repeat(12, 1fr); }
    .card { grid-column: span 4; background: var(--panel); border-radius: 16px; padding: 1.1rem; border:1px solid color-mix(in srgb, var(--panel) 70%, var(--text) 6%); box-shadow: var(--shadow); display:flex; flex-direction:column; gap:.6rem; }
    .pill { display:inline-flex; align-items:center; gap:.5rem; font-size:.8rem; padding:.35rem .6rem; border-radius:999px; background: color-mix(in srgb, var(--panel) 85%, var(--text) 5%); color: var(--muted) }
    .card p { color: var(--muted); margin: 0; }
    .card .actions { margin-top:auto; display:flex; gap:.6rem; }

    @media (max-width: 900px){ .card { grid-column: span 6; } }
    @media (max-width: 600px){ .card { grid-column: span 12; } }

    /* Gallery */
    .gallery { display:grid; grid-template-columns: repeat(6, 1fr); gap: .8rem; }
    .tile { grid-column: span 2; aspect-ratio: 4/3; border-radius: 14px; overflow:hidden; background: linear-gradient(135deg, color-mix(in srgb, var(--brand) 20%, transparent), color-mix(in srgb, var(--brand-2) 20%, transparent)); border: 1px solid color-mix(in srgb, var(--panel) 70%, var(--text) 6%); box-shadow: var(--shadow); position:relative; }
    .tile .cap { position:absolute; inset:auto 0 0 0; padding:.6rem .75rem; background: linear-gradient(180deg, transparent, rgba(0,0,0,.35)); font-weight:600; }
    .tile .blob {
      position:absolute; inset: -20% -20% auto auto; width: 60%; aspect-ratio: 1; border-radius: 50%; background: radial-gradient(circle at 30% 30%, #fff3, #fff0 55%), linear-gradient(135deg, var(--brand), var(--brand-2)); filter: blur(6px); opacity:.65;
      transform: rotate(8deg);
    }
    @media (max-width: 900px){ .tile { grid-column: span 3; } }
    @media (max-width: 600px){ .tile { grid-column: span 6; } }

    /* Footer */
    footer { border-top:1px solid color-mix(in srgb, var(--panel) 60%, var(--text) 5%); padding: 2rem 0; color: var(--muted); }

    /* Utilities */
    .hidden { display:none !important; }
    .sr-only { position:absolute; width:1px; height:1px; padding:0; margin:-1px; overflow:hidden; clip:rect(0,0,0,0); white-space:nowrap; border:0; }

    /* Floating controls */
    .fab {
      position: fixed; right: 18px; bottom: 18px; z-index: 50;
      display:flex; gap:.6rem; align-items:center;
    }
    .fab button {
      width: 42px; height: 42px; border-radius: 12px; display:grid; place-items:center; border: 1px solid color-mix(in srgb, var(--panel) 70%, var(--text) 6%);
      background: var(--panel); color: var(--text); cursor: pointer; box-shadow: var(--shadow);
    }

    /* Modal */
    .modal-backdrop { position: fixed; inset: 0; background: rgba(0,0,0,.45); display:grid; place-items:center; z-index: 60; }
    .modal { width: min(520px, 92vw); background: var(--panel); border:1px solid color-mix(in srgb, var(--panel) 70%, var(--text) 6%); border-radius: 16px; box-shadow: var(--shadow); overflow: hidden; }
    .modal header { position:static; backdrop-filter:none; background:transparent; border-bottom:1px solid color-mix(in srgb, var(--panel) 60%, var(--text) 6%); }
    .modal .content { padding: 1rem 1.1rem 1.2rem; }

    /* Toast */
    .toast { position: fixed; left: 50%; bottom: 24px; transform: translateX(-50%);
      background: var(--panel); color: var(--text); border-radius: 12px; padding: .7rem 1rem; border:1px solid color-mix(in srgb, var(--panel) 70%, var(--text) 6%); box-shadow: var(--shadow); z-index: 70; }
  </style>
</head>
<body>
  <header>
    <div class="container nav">
      <a href="#" class="brand"><span class="dot" aria-hidden="true"></span> RandomTemplate</a>
      <nav class="links">
        <a href="#features">Features</a>
        <a href="#gallery">Gallery</a>
        <a href="#cta">Get Started</a>
        <button class="ghost" id="openModalBtn">Open Modal</button>
        <button class="btn" id="themeBtn" title="Toggle Theme">Toggle Theme</button>
      </nav>
      <button class="menu-toggle" id="menuToggle" aria-label="Toggle menu">☰</button>
    </div>
    <div class="container mobile-menu" id="mobileMenu">
      <a href="#features">Features</a>
      <a href="#gallery">Gallery</a>
      <a href="#cta">Get Started</a>
      <button class="ghost" id="openModalBtnMobile">Open Modal</button>
      <button class="btn" id="themeBtnMobile">Toggle Theme</button>
    </div>
  </header>

  <main>
    <!-- Hero -->
    <section class="hero">
      <div class="container wrap">
        <div>
          <h1 class="title">A clean, modern one‑file template</h1>
          <p class="subtitle">Copy–paste and start building. Fully responsive, themeable, and sprinkled with useful UI bits: cards, gallery, modal, toast, and more.</p>
          <div class="cta">
            <a class="btn" href="#features">Explore Components</a>
            <button class="btn ghost" id="toastBtn">Show Toast</button>
          </div>
        </div>
        <div class="hero-card">
          <div class="stat" style="margin-bottom:.6rem">
            <div class="ico">⚡</div>
            <div>
              <strong>Zero setup</strong><br />
              <small>Everything lives in a single HTML file.</small>
            </div>
          </div>
          <div class="stat" style="margin-bottom:.6rem">
            <div class="ico">🎨</div>
            <div>
              <strong>Theme switch</strong><br />
              <small>Light & dark via CSS variables.</small>
            </div>
          </div>
          <div class="stat">
            <div class="ico">📱</div>
            <div>
              <strong>Responsive</strong><br />
              <small>Looks good on any screen.</small>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- Features -->
    <section id="features">
      <div class="container">
        <h2 class="section-title">Quick Components</h2>
        <p class="section-sub">Cards, actions, and pills to build sections fast. Replace text, duplicate cards, done.</p>
        <div class="grid">
          <article class="card">
            <span class="pill">New</span>
            <h3>Card Title</h3>
            <p>Short description about this feature. Keep it to two lines max for harmony.</p>
            <div class="actions">
              <a class="btn" href="#">Primary</a>
              <a class="btn ghost" href="#">Secondary</a>
            </div>
          </article>
          <article class="card">
            <span class="pill">Tips</span>
            <h3>Another Card</h3>
            <p>Swap content and duplicate this card to add more items to your grid.</p>
            <div class="actions">
              <a class="btn" href="#">Learn more</a>
              <a class="btn ghost" href="#">Docs</a>
            </div>
          </article>
          <article class="card">
            <span class="pill">Pro</span>
            <h3>Fancy Effects</h3>
            <p>Gradient blobs & soft shadows give a modern feel without any libraries.</p>
            <div class="actions">
              <a class="btn" href="#">Try it</a>
              <a class="btn ghost" href="#">Source</a>
            </div>
          </article>
        </div>
      </div>
    </section>

    <!-- Gallery / Tiles -->
    <section id="gallery">
      <div class="container">
        <h2 class="section-title">Visual Tiles</h2>
        <p class="section-sub">Use as a portfolio grid, product cards, or image placeholders. Pure CSS shapes.</p>
        <div class="gallery">
          <div class="tile"><div class="blob"></div><div class="cap">Gradient Tile</div></div>
          <div class="tile"><div class="blob" style="inset:auto auto -20% -20%"></div><div class="cap">Organic Blob</div></div>
          <div class="tile"><div class="blob" style="opacity:.5"></div><div class="cap">Soft Glow</div></div>
          <div class="tile"><div class="blob"></div><div class="cap">Vibe</div></div>
          <div class="tile"><div class="blob" style="opacity:.45"></div><div class="cap">Minimal</div></div>
          <div class="tile"><div class="blob" style="opacity:.35"></div><div class="cap">Clean</div></div>
        </div>
      </div>
    </section>

    <!-- CTA -->
    <section id="cta">
      <div class="container" style="display:grid; gap:1rem; grid-template-columns: 1.2fr 1fr; align-items:center">
        <div>
          <h2 class="section-title">Ready to build?</h2>
          <p class="section-sub">Copy this file into your project, tweak variables, and ship. No tooling required.</p>
          <div class="cta">
            <a class="btn" href="#">Get Started</a>
            <a class="btn ghost" href="#">View Docs</a>
          </div>
        </div>
        <div class="hero-card">
          <form id="demoForm">
            <label class="sr-only" for="email">Email</label>
            <input id="email" name="email" type="email" placeholder="Your email" required
                   style="width:100%; padding:.9rem 1rem; border-radius:12px; border:1px solid color-mix(in srgb, var(--panel) 70%, var(--text) 6%); background: color-mix(in srgb, var(--panel) 90%, var(--text) 2%); color: var(--text); margin-bottom:.7rem;">
            <button class="btn" type="submit" style="width:100%">Notify Me</button>
          </form>
          <small style="color:var(--muted)">We respect your inbox. No spam.</small>
        </div>
      </div>
    </section>
  </main>

  <footer>
    <div class="container">© <span id="year"></span> RandomTemplate. Built with ❤️ — replace me freely.</div>
  </footer>

  <!-- Floating buttons -->
  <div class="fab">
    <button id="scrollTop" title="Back to top">↑</button>
    <button id="themeFab" title="Toggle Theme">☀︎</button>
  </div>

  <!-- Modal -->
  <div class="modal-backdrop hidden" id="modal">
    <div class="modal" role="dialog" aria-modal="true" aria-labelledby="modalTitle">
      <header>
        <div class="container nav" style="padding:.8rem 1rem">
          <strong id="modalTitle">Quick Modal</strong>
          <button class="menu-toggle" id="closeModal" aria-label="Close">✕</button>
        </div>
      </header>
      <div class="content">
        <p>This is a lightweight modal. Put any content here — forms, images, or text.</p>
        <div class="cta" style="margin-top:1rem">
          <button class="btn" id="confirmModal">Looks Good</button>
          <button class="btn ghost" id="closeModal2">Close</button>
        </div>
      </div>
    </div>
  </div>

  <!-- Toast -->
  <div class="toast hidden" id="toast">Hello! This is a quick toast. 🎉</div>
</body>
</html>
