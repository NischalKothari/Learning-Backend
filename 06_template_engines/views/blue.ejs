<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Luminous Galaxy</title>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }
    body {
      height: 100vh;
      display: flex;
      justify-content: center;
      align-items: center;
      background: radial-gradient(circle at top, #0f2027, #203a43, #2c5364);
      overflow: hidden;
      font-family: 'Poppins', sans-serif;
      color: #fff;
    }

    /* Stars */
    .stars {
      position: absolute;
      width: 100%;
      height: 100%;
      background: transparent;
      box-shadow: 
        100px 200px #fff, 
        300px 600px #fff, 
        500px 100px #fff,
        700px 400px #fff, 
        900px 250px #fff,
        1100px 550px #fff;
      animation: twinkle 4s infinite alternate;
    }

    @keyframes twinkle {
      from { opacity: 0.5; }
      to { opacity: 1; }
    }

    .container {
      text-align: center;
      position: relative;
      z-index: 2;
    }

    .title {
      font-size: 3.5rem;
      letter-spacing: 3px;
      text-shadow: 0 0 15px #0ff, 0 0 30px #0ff, 0 0 60px #0ff;
      animation: glow 3s infinite alternate;
    }

    @keyframes glow {
      from {
        text-shadow: 0 0 10px #0ff, 0 0 20px #0ff;
      }
      to {
        text-shadow: 0 0 25px #0ff, 0 0 60px #0ff, 0 0 80px #0ff;
      }
    }

    .subtitle {
      margin-top: 10px;
      font-size: 1.2rem;
      opacity: 0.8;
      letter-spacing: 2px;
    }

    .btn {
      margin-top: 30px;
      padding: 12px 30px;
      border: 2px solid #0ff;
      border-radius: 30px;
      background: transparent;
      color: #0ff;
      font-size: 1.1rem;
      font-weight: bold;
      cursor: pointer;
      transition: all 0.3s ease;
      box-shadow: 0 0 10px #0ff, inset 0 0 10px #0ff;
    }

    .btn:hover {
      background: #0ff;
      color: #000;
      box-shadow: 0 0 20px #0ff, 0 0 40px #0ff;
    }
  </style>
</head>
<body>
  <div class="stars"></div>
  <div class="container">
    <h1 class="title">Luminous Galaxy</h1>
    <p class="subtitle">Explore the beauty of glowing designs</p>
    <button class="btn">Enter</button>
  </div>
</body>
</html>
