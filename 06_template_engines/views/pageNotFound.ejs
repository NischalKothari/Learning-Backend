<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Luminous Galaxy</title>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }
    body {
      height: 100vh;
      display: flex;
      justify-content: center;
      align-items: center;
      background: radial-gradient(circle at top, #270f1a, #43203c, #642c4b);
      overflow: hidden;
      font-family: 'Poppins', sans-serif;
      color: #fff;
    }

    /* Stars */
    .stars {
      position: absolute;
      width: 100%;
      height: 100%;
      background: transparent;
      box-shadow: 
        100px 200px #fff, 
        300px 600px #fff, 
        500px 100px #fff,
        700px 400px #fff, 
        900px 250px #fff,
        1100px 550px #fff;
      animation: twinkle 4s infinite alternate;
    }

    @keyframes twinkle {
      from { opacity: 0.5; }
      to { opacity: 1; }
    }

    .container {
      text-align: center;
      position: relative;
      z-index: 2;
    }

    .title {
      font-size: 3.5rem;
      letter-spacing: 3px;
      text-shadow: 0 0 15px hotpink, 0 0 30px hotpink, 0 0 60px hotpink;
      animation: glow 3s infinite alternate;
    }

    @keyframes glow {
      from {
        text-shadow: 0 0 10px hotpink, 0 0 20px hotpink;
      }
      to {
        text-shadow: 0 0 25px hotpink, 0 0 60px hotpink, 0 0 80px hotpink;
      }
    }

    .subtitle {
      margin-top: 10px;
      font-size: 1.2rem;
      opacity: 0.8;
      letter-spacing: 2px;
    }

    .btn {
      margin-top: 30px;
      padding: 12px 30px;
      border: 2px solid hotpink;
      border-radius: 30px;
      background: transparent;
      color: hotpink;
      font-size: 1.1rem;
      font-weight: bold;
      cursor: pointer;
      transition: all 0.3s ease;
      box-shadow: 0 0 10px hotpink, inset 0 0 10px hotpink;
    }

    .btn:hover {
      background: hotpink;
      color: #000;
      box-shadow: 0 0 20px hotpink, 0 0 40px hotpink;
    }
  </style>
</head>
<body>
  <div class="stars"></div>
  <div class="container">
    <h1 class="title"><%= err %></h1>
    <p class="subtitle">Page Not Found</p>
  </div>
</body>
</html>
